# BOOTSTRAP
```sh
en main.js: 
import 'bootstrap/dist/css/bootstrap.min.css'
import 'bootstrap/dist/js/bootstrap.bundle.min.js'

** Trabajaremos con LAYOUT
    >> Conteiners
    >> Grid: 12 columnas.
    >> Col: columnas

    >> m: margin
    >> p: padding
    >> t: top
    >> b: bottom
    >> s: start
    >> g: gatters
    >> x: horizontal
    >> y: vertical 

    etc.

```
